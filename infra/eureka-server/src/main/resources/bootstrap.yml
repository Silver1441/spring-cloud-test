spring:
  application:
    name: eureka-service-registry
  cloud:
    config:
      name: eureka-server
      uri: http://${CONFIG_HOST:localhost}:${CONFIG_PORT:8888}
      failFast: true

server.port: 8989
